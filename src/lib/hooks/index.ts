/**
 * Barcelos South Africa - Custom Hooks
 *
 * Central export point for all custom React hooks.
 * Import from '@/lib/hooks' for consistent access.
 */

// Currently no custom hooks exported
// Locations functionality removed - handled by ServeUp
